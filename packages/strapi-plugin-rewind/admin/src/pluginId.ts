/**
 * Stored in nothing user-facing yet, but the same rule applies as to any plugin
 * id: it appears in route paths and permission names, so changing it later
 * breaks installs rather than merely renaming a thing.
 */
export const PLUGIN_ID = 'rewind';
